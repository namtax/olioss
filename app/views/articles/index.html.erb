<div>
  <h1 class="font-bold text-4xl">Articles (<%= @articles.count %>)</h1>
  <p><%= Article.count %></p>
  <p><%= User.count %></p>
  <% @articles.each do |article| %>
    <%= render article %>
  <% end %>
</div>
